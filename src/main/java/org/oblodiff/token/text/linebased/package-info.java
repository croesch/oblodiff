/**
 * Contains classes that describe the textual tokens that are line orientated. In difference to
 * {@link org.oblodiff.token.text.simple} the tokens in this package divide the text into lines rather than dividing it
 * in a more semantic way.
 */
package org.oblodiff.token.text.linebased;
